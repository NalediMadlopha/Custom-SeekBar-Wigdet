<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/seek_bar_widget_left_label"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:gravity="start"
            android:padding="@dimen/seek_bar_widget_slider_padding"
            android:textColor="@android:color/darker_gray"
            android:textSize="@dimen/medium_text_size"
            tools:text="Left Label" />

        <TextView
            android:id="@+id/seek_bar_widget_right_label"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:gravity="end"
            android:padding="@dimen/seek_bar_widget_slider_padding"
            android:textColor="@android:color/darker_gray"
            android:textSize="@dimen/medium_text_size"
            tools:text="Right Label" />

    </LinearLayout>

    <androidx.appcompat.widget.AppCompatSeekBar
        android:id="@+id/seek_bar_widget_app_compat_seek_bar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

</LinearLayout>